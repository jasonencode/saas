<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 强制开启HTTPS
    |--------------------------------------------------------------------------
    */
    'force_https' => env('FORCE_HTTPS', false),

    /*
    |--------------------------------------------------------------------------
    | 多节点时，服务器 ID 标记
    |--------------------------------------------------------------------------
    */
    'server_id' => env('SERVER_ID'),

    /*
    |--------------------------------------------------------------------------
    | 域名配置
    |--------------------------------------------------------------------------
    | 默认域名 default_domain
    | api域名 api_domain
    | 后台域名 backend_domain
    | 租户域名 tenant_domain
    */
    'domains' => [
        'default_domain' => env('DEFAULT_DOMAIN', ''),
        'api_domain' => env('API_DOMAIN', ''),
        'backend_domain' => env('BACKEND_DOMAIN', ''),
        'tenant_domain' => env('TENANT_DOMAIN', ''),
    ],

    /*
    |--------------------------------------------------------------------------
    | 默认租户用户密码
    |--------------------------------------------------------------------------
    */
    'tenant_user_password' => env('TENANT_USER_PASSWORD', 'a123456'),

    /*
    |--------------------------------------------------------------------------
    | 表使用外键
    |--------------------------------------------------------------------------
    */
    'table_use_foreign' => env('TABLE_USE_FOREIGN', true),

    /*
    |--------------------------------------------------------------------------
    | 用户身份配置
    |--------------------------------------------------------------------------
    | allow_multiple: 是否允许用户同时拥有多个身份
    |   true  - 用户可同时订阅多个身份（叠加有效期）
    |   false - 用户同一时间只能拥有一个身份，订阅新身份时自动替换旧身份
    */
    'identity' => [
        'allow_multiple' => env('IDENTITY_ALLOW_MULTIPLE', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | 商城配置
    |--------------------------------------------------------------------------
    */
    'mall' => [
        'order_expired_minutes' => env('MALL_ORDER_EXPIRED_MINUTES', 30),
    ],

    /*
    |--------------------------------------------------------------------------
    | 区块链模块配置
    |--------------------------------------------------------------------------
    */
    'block_chain' => [
        'public_key' => env('BLOCK_CHAIN_PUBLIC_KEY'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 频率限制配置
    |--------------------------------------------------------------------------
    */
    'rate_limits' => [
        'api' => env('RATE_LIMIT_API', 60),
        'upload' => env('RATE_LIMIT_UPLOAD', 10),
        'login' => env('RATE_LIMIT_LOGIN', 5),
        'sms' => env('RATE_LIMIT_SMS', 2),
        'register' => env('RATE_LIMIT_REGISTER', 3),
        'password_reset' => env('RATE_LIMIT_PASSWORD_RESET', 3),
        'default' => env('RATE_LIMIT_DEFAULT', 30),
    ],
];
